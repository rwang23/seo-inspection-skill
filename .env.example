# Copy to .env in this skill directory or place the same keys in the project-root .env.
# Skill-local .env takes precedence over the project-root .env.

GSC_SERVICE_ACCOUNT_PATH=credential/google-service-account.json
# Optional alias if the same service account is shared across GSC and GA4.
GOOGLE_SERVICE_ACCOUNT_PATH=credential/google-service-account.json
GSC_SITE_URL=sc-domain:example.com
GSC_LANGUAGE_CODE=en-US
GA4_PROPERTY_ID=123456789
